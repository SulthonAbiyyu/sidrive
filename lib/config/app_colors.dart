import 'package:flutter/material.dart';

/// ===========================================================================
/// APP COLORS - Light & Dark Mode
/// ============================================================================
class AppColors {
  // ──────────────────────────────────────────
  // LIGHT MODE
  // ──────────────────────────────────────────
  static const Color primaryLight = Color(0xFF5B7FFF);
  static const Color primaryLightVariant = Color(0xFF4A6FEE);

  static const Color secondaryLight = Color(0xFFFFB800);
  static const Color secondaryLightVariant = Color(0xFFFFA000);

  static const Color backgroundLight = Color(0xFFFFFFFF);
  static const Color surfaceLight = Color(0xFFF8F9FE);
  static const Color cardLight = Color(0xFFFFFFFF);

  static const Color textPrimaryLight = Color(0xFF1A1D1F);
  static const Color textSecondaryLight = Color(0xFF6F767E);

  static const Color borderLight = Color(0xFFE4E7EB);
  static const Color dividerLight = Color(0xFFE4E7EB);

  static const Color successLight = Color(0xFF34C759);
  static const Color warningLight = Color(0xFFFFB800);
  static const Color errorLight = Color(0xFFFF3B30);
  static const Color infoLight = Color(0xFF5B7FFF);

  static const Color disabledLight = Color(0xFFC7CCD1);

  // Shimmer
  static const Color shimmerBaseLight = Color(0xFFE0E0E0);
  static const Color shimmerHighlightLight = Color(0xFFF5F5F5);

  // ──────────────────────────────────────────
  // DARK MODE
  // ──────────────────────────────────────────
  static const Color primaryDark = Color(0xFF5B7FFF);
  static const Color primaryDarkVariant = Color(0xFF6B8FFF);

  static const Color secondaryDark = Color(0xFFFFB800);
  static const Color secondaryDarkVariant = Color(0xFFFFC233);

  static const Color backgroundDark = Color(0xFF111315);
  static const Color surfaceDark = Color(0xFF1A1D1F);
  static const Color cardDark = Color(0xFF252A2E);

  static const Color textPrimaryDark = Color(0xFFFCFCFC);
  static const Color textSecondaryDark = Color(0xFF9A9FA5);

  static const Color borderDark = Color(0xFF33383F);
  static const Color dividerDark = Color(0xFF33383F);

  static const Color successDark = Color(0xFF34C759);
  static const Color warningDark = Color(0xFFFFB800);
  static const Color errorDark = Color(0xFFFF3B30);
  static const Color infoDark = Color(0xFF5B7FFF);

  static const Color disabledDark = Color(0xFF6F767E);

  static const Color shimmerBaseDark = Color(0xFF252A2E);
  static const Color shimmerHighlightDark = Color(0xFF33383F);
}